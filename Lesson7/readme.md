# Lesson 7 — Functions, export/import(typescript)

This lesson covers creating functions in JavaScript, including regular and arrow functions. npx
Functions are used to process data (e.g., arrays) and return results.

It also introduces basic concepts of `export` and `import` for organizing code across multiple files.
``
